<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pinknote">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_my"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.pinknote.AboutActivity"
            android:parentActivityName="com.example.pinknote.MainActivity" />
        <activity
            android:name="com.example.pinknote.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName="com.example.pinknote.MainActivity" />
        <activity
            android:name="com.example.pinknote.AddEditNoteActivity"
            android:parentActivityName="com.example.pinknote.MainActivity" />
        <activity
            android:name="com.example.pinknote.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>