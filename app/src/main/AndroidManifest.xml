<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_my"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_my"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.elvina.pinknote.ui.AboutActivity"
            android:parentActivityName="com.elvina.pinknote.ui.MainActivity" />
        <activity
            android:name="com.elvina.pinknote.ui.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName="com.elvina.pinknote.ui.MainActivity" />
        <activity
            android:name="com.elvina.pinknote.ui.AddEditNoteActivity"
            android:parentActivityName="com.elvina.pinknote.ui.MainActivity" />
        <activity
            android:exported="true"
            android:name="com.elvina.pinknote.ui.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>